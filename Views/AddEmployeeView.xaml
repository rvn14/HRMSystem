<UserControl x:Class="HRM_System.Views.AddEmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HRM_System.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Main Content -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Back Button -->
            <Button Content="← Back" Width="80" Margin="0,0,0,10" Click="BackButton_Click" />

            <!-- Form Section -->
            <Border Grid.Row="1" Background="WhiteSmoke" Padding="20" CornerRadius="10" BorderBrush="#DDDDDD" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <TextBlock Grid.Row="0" Text="EMPLOYEE DETAILS" FontWeight="Bold" FontSize="18" Margin="0,0,0,20" 
                                   HorizontalAlignment="Center" Foreground="#333333"/>

                        <!-- Form Fields - Single Column Layout -->
                        <StackPanel Grid.Row="1" Width="Auto" MaxWidth="450">
                            <!-- Name -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Name" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="NameBox" Padding="8" BorderThickness="1"/>
                            </StackPanel>

                            <!-- Employee ID -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Employee ID" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="EmployeeIdBox" Padding="8" BorderThickness="1"/>
                            </StackPanel>

                            <!-- Email -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="EmailBox" Padding="8" BorderThickness="1"/>
                            </StackPanel>

                            <!-- Contact Number -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Contact Number" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="ContactBox" Padding="8" BorderThickness="1"/>
                            </StackPanel>

                            <!-- Date of Birth -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Date of Birth" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <DatePicker x:Name="DobPicker" Padding="5" BorderThickness="1" Width="Auto"/>
                            </StackPanel>

                            <!-- Job Position -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Job Position" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="PositionBox" Padding="8" BorderThickness="1"/>
                            </StackPanel>

                            <!-- Department -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Department" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="DepartmentBox" Padding="8" BorderThickness="1"/>
                            </StackPanel>

                            <!-- Recruitment Date -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Recruitment Date" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <DatePicker x:Name="RecruitmentDatePicker" Padding="5" BorderThickness="1" Width="Auto"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Buttons -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                            <Button Content="RESET" Width="120" Height="35" Margin="0,0,15,0" 
                                     Foreground="White" 
                                    Click="ResetButton_Click"/>
                            <Button Content="SUBMIT" Width="120" Height="35" 
                                     Foreground="White" 
                                    Click="SubmitButton_Click"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
