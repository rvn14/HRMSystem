<UserControl x:Class="HRM_System.Views.EmployeeLeaveRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <!-- Sidebar -->
            <ColumnDefinition Width="*"/>
            <!-- Main -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#F0F0F0">
            <StackPanel Margin="10">
                <Border BorderBrush="Blue" BorderThickness="2" Padding="10">
                    <StackPanel>
                        <Ellipse Width="60" Height="60" Fill="Gray" HorizontalAlignment="Left" Margin="0,5"/>
                        <TextBlock Text="WELCOME" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="USER ID – 463JDIO" FontSize="12"/>
                    </StackPanel>
                </Border>
                <Button Content="ATTENDENCE" Height="40" Margin="0,30,0,0" Background="Black" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Main Form -->
        <Border Grid.Column="1" Margin="10" Background="#FFF" CornerRadius="10" Padding="20" BorderBrush="#DDD" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <!-- Form -->
                    <RowDefinition Height="Auto"/>
                    <!-- Buttons -->
                </Grid.RowDefinitions>

                <!-- Request Form -->
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="250"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="EMPLOYEE ID:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="EMPLOYEE NAME:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="JOB POSITION:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,5"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="CAUSE FOR THE LEAVE:" VerticalAlignment="Top" Margin="0,5"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="TYPE OF LEAVE:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Margin="0,5"/>
                    <Button Grid.Row="4" Grid.Column="2" Content="..." Width="30" Margin="5,0"/>
                </Grid>

                <!-- Footer Buttons -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="RESET" Width="100" Height="35" Background="#888" Foreground="White"/>
                    <Button Content="SUBMIT" Width="100" Height="35" Background="#444" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
