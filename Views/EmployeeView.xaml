<UserControl x:Class="HRM_System.Views.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <!-- Sidebar -->
            <ColumnDefinition Width="*"/>
            <!-- Main Content -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar Placeholder -->
        <Border Grid.Column="0" Background="#FFEFEFEF">
            <StackPanel Margin="10">
                <!-- Profile -->
                <Ellipse Width="60" Height="60" Fill="Gray" HorizontalAlignment="Left" Margin="0,10"/>
                <TextBlock Text="WELCOME" FontSize="16" FontWeight="Bold" Margin="0,10,0,0"/>
                <TextBlock Text="USER ID - 463JDIO" FontSize="12"/>

                <!-- Sidebar Navigation Placeholder -->
                <Button Content="Employees" Margin="0,30,0,0" Height="40" Background="Black" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <!-- Table Area -->
                <RowDefinition Height="Auto"/>
                <!-- Buttons -->
                <RowDefinition Height="Auto"/>
                <!-- Search -->
            </Grid.RowDefinitions>

            <!-- Employee Table -->
            <Border Grid.Row="0" Background="#FFF5F5F5" BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="8">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="Employee table goes here"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="Gray"
                               Padding="20"/>
                </ScrollViewer>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
                <Button Content="ADD EMPLOYEE" Width="130" Height="35" Background="MediumPurple" Foreground="White"/>
                <Button Content="REMOVE EMPLOYEE" Width="130" Height="35" Background="MediumPurple" Foreground="White"/>
                <Button Content="EDIT EMPLOYEE" Width="130" Height="35" Background="MediumPurple" Foreground="White"/>
            </StackPanel>

            <!-- Search Field -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Center">
                <Label Content="SEARCH EMPLOYEE" VerticalAlignment="Center"/>
                <TextBox Width="200" Height="25" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
