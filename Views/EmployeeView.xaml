<UserControl x:Class="HRM_System.Views.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HRM_System.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            
            <ColumnDefinition Width="*"/>
            <!-- Main content -->
        </Grid.ColumnDefinitions>

       

        <!-- Main Content -->
        <Grid Grid.Column="1" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Table Section -->
            <Border Grid.Row="0" Background="#F2F2F2" CornerRadius="5" Padding="20" BorderBrush="#CCC" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <DataGrid x:Name="EmployeeDataGrid" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              HeadersVisibility="Column"
                              RowBackground="White"
                              AlternatingRowBackground="#F9F9F9"
                              BorderThickness="1"
                              GridLinesVisibility="All">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding EmployeeId}" Width="Auto" />
                            <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="Auto" />
                            <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="Auto" />
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="Auto" />
                            <DataGridTextColumn Header="Contact Number" Binding="{Binding ContactNumber}" Width="Auto" />
                            <DataGridTextColumn Header="Job Role" Binding="{Binding JobRole}" Width="Auto" />
                            <DataGridTextColumn Header="Date of Birth" Binding="{Binding DateOfBirth, StringFormat=dd/MM/yyyy}" Width="Auto" />
                            <DataGridTextColumn Header="Recruitment Date" Binding="{Binding RecruitementDate, StringFormat=dd/MM/yyyy}" Width="Auto" />
                            <DataGridTemplateColumn Header="Actions" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Edit" Width="50" Margin="0,0,5,0" 
                                                    Background="#4D2D9C" Foreground="White"
                                                    Click="EditEmployee_Click" />
                                            <Button Content="Delete" Width="50" 
                                                    Background="#FF5252" Foreground="White"
                                                    Click="DeleteEmployee_Click" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </Border>

            <!-- Add Button -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,10">
                <Button Content="ADD EMPLOYEE" Width="150" Height="35" Background="#4D2D9C" Foreground="White" Click="Click_Add_Employee"/>
                <Button Content="BACK" Margin="80,0,0,0" Width="150" Height="35" Background="#4D2D9C" Foreground="White" Click="Click_Back_Button"/>
            </StackPanel>

            <!-- Search Bar -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                <TextBlock Text="SEARCH EMPLOYEE" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBox x:Name="SearchTextBox" Width="650" Height="30" TextChanged="SearchTextBox_TextChanged"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
