<UserControl x:Class="HRM_System.Views.LeaveRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HRM_System.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Main Form Container -->
        <Border Grid.Row="1" Grid.Column="1" 
                Background="#eff2f5" CornerRadius="5" 
                MinWidth="400" MaxWidth="600"
                HorizontalAlignment="Center">
            <StackPanel Margin="30,20">
                <!-- Header -->
                <TextBlock Text="REQUEST FOR A LEAVE" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <!-- Form Fields -->
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="EMPLOYEE ID" 
                               VerticalAlignment="Center" 
                               FontSize="12"/>
                    <TextBox Grid.Column="1" 
                             Height="25" 
                             VerticalContentAlignment="Center"/>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="EMPLOYEE NAME" 
                               VerticalAlignment="Center"
                               FontSize="12"/>
                    <TextBox Grid.Column="1" 
                             Height="25" 
                             VerticalContentAlignment="Center"/>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="JOB POSITION" 
                               VerticalAlignment="Center"
                               FontSize="12"/>
                    <TextBox Grid.Column="1" 
                             Height="25" 
                             VerticalContentAlignment="Center"/>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="CAUSE FOR THE LEAVE" 
                               TextWrapping="Wrap"
                               VerticalAlignment="Center"
                               FontSize="12"/>
                    <TextBox Grid.Column="1" 
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto" Margin="0,10,0,0"/>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="NUMBER OF DAYS" 
                               VerticalAlignment="Center"
                               FontSize="12"/>
                    <TextBox Grid.Column="1" 
                             Height="25" 
                             VerticalContentAlignment="Center"
                                />
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="TYPE OF LEAVE" 
                               VerticalAlignment="Center"
                               FontSize="12"/>
                    <TextBox Grid.Column="1" 
                             Height="25" 
                             VerticalContentAlignment="Center"/>

                </Grid>

                <!-- Buttons -->
                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="RESET" 
                            Height="35" 
                            
                            Foreground="White"
                            BorderThickness="0"/>

                    <Button Grid.Column="2" 
                            Content="SUBMIT" 
                            Height="35"
                            
                            Foreground="White"
                            BorderThickness="0"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
