<Window x:Class="HRM_System.Views.UserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:HRM_System.Views"
        mc:Ignorable="d" 
        FontFamily="pack://application:,,,/HRM_System;component/Fonts/#Inter" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Topmost="True"
        Title="UserView" Height="800" Width="1300">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <!-- Sidebar -->
            <ColumnDefinition Width="*"/>
            <!-- Main Content -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#F5F5F7" CornerRadius="0,15,0,0">
            <StackPanel>
                <!-- User Info -->
                <StackPanel Margin="10" HorizontalAlignment="Center">
                    <Ellipse Width="60" Height="60" Fill="Gray" />
                    <TextBlock Text="WELCOME" FontWeight="Bold" FontSize="16" Foreground="Black" Margin="0,10,0,0" />
                    <TextBlock Text="USER ID - 463JDIO" FontSize="14" Foreground="Black" />
                </StackPanel>

                <!-- Navigation Buttons -->
                <StackPanel Margin="10,20,10,0">
                    <!-- Dashboard -->
                    <Button Height="50" Margin="0,5,0,0" Background="MediumPurple" BorderBrush="Transparent" Click="DashboardButton_Click">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Dashboard" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>

                    <!-- Account Details -->
                    <Button Height="50" Margin="0,5,0,0" Background="MediumPurple" BorderBrush="Transparent" Click="AccountDetailsButton_Click">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Account Details" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>

                    <!-- Attendance -->
                    <Button Height="50" Margin="0,5,0,0" Background="MediumPurple" BorderBrush="Transparent" Click="AttendanceButton_Click">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Attendance" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>

                    <!-- Payrolls -->
                    <Button Height="50" Margin="0,5,0,0" Background="MediumPurple" BorderBrush="Transparent" Click="PayrollsButton_Click">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Payrolls" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>

                    <!-- Sign-out -->
                    <Button Height="50" Margin="0,5,0,0" Background="Transparent" BorderBrush="Transparent" Click="SignOutButton_Click">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Sign-out" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Border Grid.Column="1" Background="White">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="100"/>
                </Grid.RowDefinitions>

                <!-- Content Area -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Pending Leave Requests -->
                    <Border Grid.Column="0" Margin="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
                        <TextBlock Text="PENDING LEAVE REQUESTS" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>

                    <!-- Due Meetings/Notifications -->
                    <Border Grid.Column="1" Margin="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
                        <TextBlock Text="DUE MEETINGS\nNOTIFICATIONS" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" />
                    </Border>

                    <!-- Another Pending Leave Requests -->
                    <Border Grid.Column="2" Margin="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
                        <TextBlock Text="PENDING LEAVE REQUESTS" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </Grid>

                <!-- Search Bar -->
                <Border Grid.Row="1" Margin="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
                    <TextBox Text="SEARCH BY KEY WORD" VerticalAlignment="Center" Margin="10" />
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
